/*
 * wifi_config.h.example — WiFi credentials template
 *
 * Copy this to wifi_config.h and fill in your network details.
 * wifi_config.h is gitignored and will not be committed.
 */
#define WIFI_DEFAULT_SSID  "YourSSID"
#define WIFI_DEFAULT_PASS  "YourPassword"
